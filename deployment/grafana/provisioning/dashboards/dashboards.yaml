apiVersion: 1

providers:
  - name: llm-observatory
    orgId: 1
    folder: LLM Observatory
    folderUid: llm-observatory
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false

